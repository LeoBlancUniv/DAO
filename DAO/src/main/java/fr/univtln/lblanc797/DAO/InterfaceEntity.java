package fr.univtln.lblanc797.DAO;

/**
 * this interface is used to make sure that every object that has to be inserted in the DB has an ID
 */

public interface InterfaceEntity {
    long getId();
}
